# -*- coding: utf-8 -*-
"""Train.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1b6KCej93YUtn-ST1B0PwpeaIswUpsPKW
"""

#importando librerias
from sklearn.model_selection import train_test_split
from sklearn.linear_model import LinearRegression
from sklearn import metrics
import pandas as pd
import numpy as np
import pickle
import os

# Cargar la data de entreanmiento y entrenarla
def load_train_data(name_x_train,name_y_train):
    X_train = pd.read_csv(os.path.join('../data/', name_x_train))
    Y_train = pd.read_csv(os.path.join('../data/', name_y_train))
    #entrenando el modelo
    slr= LinearRegression()
    slr.fit(X_train, Y_train)
    print("el modelo ha sido entrenado")
    #guardando el modelo
    model_path = os.path.join('../models/', model_selected)
    with open(model_path, 'wb') as file:
        pickle.dump(slr, file)
    print(f"El modelo ha sido guardado en {model_path}")
    return slr